CREATE TABLE work_days (
    work_date DATE PRIMARY KEY
);

WITH numbered_days AS (
    SELECT work_date, 
           ROW_NUMBER() OVER (ORDER BY work_date) AS rn
    FROM work_days
)
SELECT work_date
FROM numbered_days
WHERE rn % 6 = 0;