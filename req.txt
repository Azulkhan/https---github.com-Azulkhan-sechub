
$scope.signDocument = function(document) {
    // Здесь должен быть код для подписания документа с использованием ЭЦП
    // Например, вызов API для подписания документа
    
    // После успешного подписания, выполнить какие-то действия, например, отправить подписанный документ на сервер
    $http.post('/api/signDocument', { signedDocument: signedDocument })
        .then(function(response) {
            // Обработка успешного ответа от сервера
            console.log("Документ успешно подписан и отправлен на сервер.");
        })
        .catch(function(error) {
            // Обработка ошибки
            console.error("Ошибка при отправке подписанного документа на сервер:", error);
        });
};
