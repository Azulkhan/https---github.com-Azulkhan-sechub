Debug.WriteLine("Количество строк: " + menuTable.Rows.Count);
foreach (DataRow row in menuTable.Rows)
{
    Debug.WriteLine("BLOK_ID: " + row["BLOK_ID"].ToString());
    // Используйте подходящий метод для вывода данных в вашей среде
}
