Для добавления апострофа в начале значения переменной `sap_barcode` в шаблоне, можно использовать функцию `printf` для форматирования строки. Например:

```lua
{{ printf "'%s'" (index $item_value "sap_barcode") }}
```

Этот код добавит апостроф перед значением переменной `sap_barcode`.